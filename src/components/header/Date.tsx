import { motion } from "framer-motion";

export const Date = () => {
  return (
    <motion.div className="my-5 text-4xl">
      Este <b>Sábado 10 de Junio, 14hs</b>
    </motion.div>
  );
};

export default Date;
